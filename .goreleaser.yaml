version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: git-sage
    main: ./cmd/git-sage
    binary: git-sage
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X git-sage/internal/version.Version={{ .Version }}
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - id: default
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
